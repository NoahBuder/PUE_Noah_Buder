import numpy as np

def bubble_sort(the_array_to sort):

    temp_array = the_array_to_sort.copy()
    while sorted(test_array) != temp_array:
        temp_array.random.shuffle 
        

    return[]


if__name_=="__main__":
    test_array = np.array([5, 2, 9, 1, 5, 6])
    print(bubble_sort(test_array))